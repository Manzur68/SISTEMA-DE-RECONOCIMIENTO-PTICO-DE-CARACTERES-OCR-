# ─────────────────────────────────────────────
# requirements-vm1.txt
# Dependencias para VM1: API Gateway + Microservicios de Calificación y Reporte
# ─────────────────────────────────────────────

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9     # Necesario para File upload en FastAPI

# Validación de datos
pydantic==2.7.1
pydantic-settings==2.2.1

# Mensajería RabbitMQ
pika==1.3.2

# Base de datos PostgreSQL
sqlalchemy==2.0.30
psycopg2-binary==2.9.9       # Driver PostgreSQL

# Utilidades
python-dotenv==1.0.1
httpx==0.27.0                # Cliente HTTP (útil para tests/health checks)
